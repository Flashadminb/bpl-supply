# BPL SUPPLY — คำสั่งประจำโปรเจกต์

ระบบเบิก–คืนวัสดุสิ้นเปลืองในฮับขนส่ง ใช้งานหลักบนมือถือของพนักงานหน้างาน
เอกสารอ้างอิง: `docs/SPEC.md` (สเปกเต็ม), `docs/schema.sql` (ฐานข้อมูล), `docs/design-tokens.css` (ดีไซน์)

## Tech stack — ห้ามเปลี่ยนโดยไม่ถาม

- Frontend: React + TypeScript + Vite + Tailwind, PWA ผ่าน `vite-plugin-pwa`
- Backend: Supabase (Postgres + Auth + Edge Functions) — ทุกอย่างต้องอยู่ใน free tier
- รูปหลักฐาน: **ไม่เก็บใน Supabase Storage** ส่งตรงเข้า Google Drive ขององค์กร เก็บแค่ `drive_file_id` + `web_link` ในฐานข้อมูล
- รายงาน: Google Sheets API ผ่าน service account
- Deploy: Cloudflare Pages

## กติกาที่ห้ามพลาด

1. **ตัดสต็อกผ่าน RPC `create_requisition` เท่านั้น** ห้าม `update items set qty_on_hand = ...` ตรง ๆ จากฝั่ง client ฟังก์ชันนี้ล็อกแถวด้วย `FOR UPDATE` และล้มทั้งคำขอถ้ามีบรรทัดใดสต็อกไม่พอ
2. **1 คำขอ = หลายรายการเสมอ** อย่าออกแบบ API แบบรายการเดียว โครงคือ `requisitions` (หัว) + `requisition_items` (บรรทัด)
3. **รูปหลักฐานผูกกับคำขอ ไม่ใช่ผูกกับรายการ** แนบได้สูงสุด 5 ใบต่อคำขอ (เดิมกำหนดไว้ 1 ใบ เจ้าของระบบขอเพิ่มภายหลัง)
   ใบแรกเป็น "รูปหลัก" เก็บใน `requisitions.evidence_file_id` เหมือนเดิม ใบที่เหลืออยู่ในตาราง `requisition_photos`
   อะไรที่อ่าน `evidence_file_id` อยู่เดิมจึงยังทำงานได้ทั้งหมด
4. **พนักงานหน้างานเปิดดูรูปหลักฐานไม่ได้** ห้ามมีลิงก์ Drive โผล่ในหน้าฝั่ง staff — เห็นได้แค่แถบรหัสสถานะย่อ
5. **บังคับสิทธิ์ที่ RLS ไม่ใช่แค่ซ่อนปุ่ม** ทุกตารางต้องมี policy ครบ
6. Service account key อยู่ใน Edge Function secrets เท่านั้น **ห้ามหลุดเข้า bundle ฝั่ง client เด็ดขาด**
7. ลบ blob/object URL ของรูปในเครื่องทันทีที่อัปโหลดสำเร็จ

## แถบรหัสสถานะย่อ (แสดงบนหน้าพนักงาน)

ชิปเล็ก ตัวอักษร monospace 10px เรียงเป็นแถวเดียว ต่อท้ายด้วยเลขคำขอและเวลา
ไว้ให้พนักงานแคปหน้าจอส่งมาแล้วแอดมินอ่านออกว่าค้างขั้นไหน

| รหัส | ความหมาย |
|---|---|
| `IMG` | ย่อ/บีบอัดรูปในเครื่อง |
| `GDV` | อัปโหลดเข้า Google Drive |
| `SPB` | บันทึก Supabase + ตัดสต็อก |
| `TMP` | ลบไฟล์ชั่วคราวในเครื่อง |
| `GSH` | ส่งลง Google Sheet แล้ว |

สัญลักษณ์: `✓` สำเร็จ (เขียว) · `…` กำลังทำ (เหลือง) · `—` ยังไม่ถึงคิว (เทา)

## ภาษาและข้อความ

UI เป็นภาษาไทย มีอังกฤษกำกับเฉพาะป้ายกำกับฟอร์มและหัวข้อสำคัญ
โค้ด คอมเมนต์ ชื่อตัวแปร ชื่อไฟล์ เป็นอังกฤษทั้งหมด

## ข้อจำกัดหน้างานที่ต้องคิดถึงเสมอ

- ใช้กลางแดด ตัวอักษรบนพื้นเหลืองต้องเป็นสีเข้มเสมอ ห้ามขาวบนเหลือง
- พนักงานใส่ถุงมือ ปุ่มกดต้องไม่ต่ำกว่า 44×44px
- เน็ตในฮับไม่นิ่ง ทุก action ต้องมี loading state และ retry ที่ปลอดภัย (idempotent)

## Definition of done ของแต่ละหน้า

รันได้จริงต่อกับ Supabase · responsive 390px ขึ้นไป · มี loading/empty/error state ครบ · ใช้ token จาก `design-tokens.css` ไม่ hardcode สี · ผ่าน `npm run build` โดยไม่มี type error
